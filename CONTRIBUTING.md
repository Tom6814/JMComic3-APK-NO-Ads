# 🤝 贡献指南

感谢你对这个项目的关注喵！不管你是来报 bug、提建议还是提交代码，都欢迎。

在开始之前，花 30 秒了解一下这个项目的特殊性——

## 这不是普通的开源项目

这个仓库里装的不是"源码"，而是**解包后的 APK 内部文件**（minified React chunks、混淆后的 JS、编译好的资源）。代码长这样：

```javascript
(0,N.jsx)(E.A,{adKey:"app_home_top",adIndex:e})
```

不是这样：

```javascript
<AdBanner position="home_top" index={index} />
```

这意味着：
- 没有可读的变量名，没有注释，没有格式化
- 改一个广告位可能需要追踪 3-5 个 chunk 文件
- 删除一行代码可能导致整个页面白屏

**但你不需要自己手动改。** 项目配有 [AI Skill](https://github.com/Tom6814/jmcomic-apk-mod-skill)，接入 AI 编程工具后可以自动完成修改流程。如果只是想要一个去广告的 APK，[Releases](https://github.com/Tom6814/JMComic3-APK-NO-Ads/releases) 页面直接下载就行喵。

---

## 贡献方式

### 🐛 报告 Bug

发现问题了？太感谢了喵，Bug 报告是最有价值的贡献 (ง •_•)ง

**提交 Issue 的时候说一下这些就行**：
- 下的哪个 APK（普通版 / 去游戏电影版）
- 什么手机、什么系统（比如 "小米 14 / HyperOS 2.0"）
- 怎么触发的（越详细越好喵）
- 有截图或录屏就更好了

不用写"环境：xxx"、"版本号：xxx"这种形式化的东西，用你自己的话描述清楚就行。

→ [提交 Bug Report](../../issues/new)

### 💡 功能建议

"能不能把 XX 也去掉？" "能不能加个 XX 功能？"

开个 Issue 说就行，不用顾虑喵。

不过得提前说清楚：不是所有建议都能实现。有些东西受限于原版 APK 的结构，硬改可能导致更多问题。我会如实回复能做还是不能做、为什么——不画饼，不敷衍。

→ [提建议](../../issues/new)

### 📝 改进文档

README、Skill 文档、贡献指南——文档改进永远欢迎喵！

文档在 [jmcomic-apk-mod-skill](https://github.com/Tom6814/jmcomic-apk-mod-skill) 独立仓库，往那边发 PR 就好。

### 💻 提交代码

想直接上手改代码？先别急，看完这几条再动手 (｡•̀ᴗ-)✧

1. **先开 Issue，再写代码。** 别闷头写完了才发现方向不对，那就白忙活了喵。
2. **一个 PR 一件事。** "去掉 banner"是一个 PR，"去掉文字链接"是另一个。混在一起我看不过来。
3. **别格式化代码。** 不要跑 Prettier、不要统一缩进——这些 minified 文件一格式化就坏了。
4. **改的是字符串，不是架构。** 你面对的是混淆后的 JS，PR 里应该是精确的字符串替换，不是"重构广告模块"。

**发 PR 前自查一下**：
- [ ] 开过 Issue 并且聊过了
- [ ] 只动了一个广告位 / 一个板块
- [ ] 每步改动都有 git commit（炸了能回滚）
- [ ] 在真机或模拟器上跑过，确认没问题
- [ ] 没碰到 Cookie、暗色模式、阅读器、网络请求相关的代码

**提交信息随便写，说清楚就行**：

```
git commit -m "fix: 去掉首页顶部banner广告"
```

就这一句话，不用写小作文。

→ [发 PR](../../pulls)

---

## 沟通风格

- 中文英文都行，用你最舒服的语言
- 别"您好""感谢您的宝贵时间"了，直接说事 (￣▽￣)
- 我的回复一般很短："收到，正在排查" / "已修复喵" / "这个做不了，因为..."
- 48 小时内没回就 @ 我，可能消息太多漏掉了

---

## 关于"喵"

这是我的口癖，不是项目规定。你贡献的时候不用跟着喵——当然你想喵也可以喵，不拦着 (｡･ω･｡)

---

## 致谢

每一个报 bug 的、提 PR 的、点 Star 的——都在让这个项目变好。谢谢你喵 🥰

这份指南本身也欢迎改。哪里不清楚、哪里有遗漏，直接提 Issue 或 PR。

**有问题？开 Issue。想聊天？开 Discussion。总之别憋着！** 😛
