# 🤝 贡献指南

感谢你对这个项目的关注喵！不管你是来报 bug、提建议还是提交代码，都欢迎。

在开始之前，花 30 秒了解一下这个项目的特殊性——

## 这不是普通的开源项目

这个仓库里装的不是"源码"，而是**解包后的 APK 内部文件**（minified React chunks、混淆后的 JS、编译好的资源）。代码长这样：

```javascript
(0,N.jsx)(E.A,{adKey:"app_home_top",adIndex:e})
```

不是这样：

```javascript
<AdBanner position="home_top" index={index} />
```

这意味着：
- 没有可读的变量名，没有注释，没有格式化
- 改一个广告位可能需要追踪 3-5 个 chunk 文件
- 删除一行代码可能导致整个页面白屏

**但你不需要自己手动改。** 项目配有 [AI Skill](https://github.com/Tom6814/jmcomic-apk-mod-skill)，接入 AI 编程工具后可以自动完成修改流程。如果只是想要一个去广告的 APK，[Releases](https://github.com/Tom6814/JMComic3-APK-NO-Ads/releases) 页面直接下载就行喵。

---

## 贡献方式

### 🐛 报告 Bug

发现问题了？太好了，这是最有价值的贡献之一。

**提交 Issue 时请包含**：
- 你下载的是哪个 APK（普通版 / 去游戏电影版）
- 设备型号和系统版本（如 "小米 14 / HyperOS 2.0"）
- 复现步骤（越详细越好）
- 截图或录屏（如果有的话）

不需要写"环境：xxx"、"版本号：xxx"这种形式化的东西，用你自己的话描述清楚就行。

→ [提交 Bug Report](../../issues/new)

### 💡 功能建议

想要什么新功能？或者觉得哪里可以改进？

开个 Issue 描述你的想法就行。可以是"能不能把 XX 也去掉"，也可以是"能不能加个 XX 功能"。

不过要提前说明：不是所有建议都能实现。有些功能受限于原始 APK 的架构，强行改可能导致更多问题。我会在 Issue 里如实回复能不能做、为什么能做/不能做。

→ [提建议](../../issues/new)

### 📝 改进文档

README、Skill 文档、或者这份贡献指南——文档改进永远欢迎。

文档在 [jmcomic-apk-mod-skill](https://github.com/Tom6814/jmcomic-apk-mod-skill) 独立仓库，请往那边发 PR。

### 💻 提交代码

**先别急，看清规则再动手：**

1. **先开 Issue 讨论，再写代码。** 不要在没沟通的情况下直接发 PR，可能方向就错了。
2. **尊重这个项目的本质。** 你改的是 minified JS 文件，不是源码。PR 中应该是精确的字符串替换，不是"重构广告模块"。
3. **一个 PR 做一件事。** 比如"去掉首页 banner"是一个 PR，"去掉文字链接广告"是另一个 PR。混在一起我没法审查。
4. **不要格式化代码。** 不要跑 Prettier、不要统一缩进——这些 minified 文件一旦格式化就破坏结构了。

**PR 检查清单**：
- [ ] Issue 已经开了并讨论过了
- [ ] 只改了一个广告位 / 一个板块
- [ ] 每步修改都有 git commit（方便出问题时回滚）
- [ ] 自己测试过了（在真机或模拟器上跑过）
- [ ] 没动 Cookie、暗色模式、阅读器、网络请求相关的代码

**提交格式**：

```
git commit -m "fix: 去掉首页顶部banner广告"
```

不需要写一堆描述，一句话说清楚改了什么就行。

→ [发 PR](../../pulls)

---

## 沟通风格

- 中文、英文都行，用你最舒服的语言
- 不需要"您好""感谢您的宝贵时间"——直接说事
- 我的回复通常短而直接（"收到，正在排查" / "已修复" / "这个做不了，因为..."）
- 如果 48 小时内没回复，可能是没看到，@ 我一下

---

## 关于"喵"

这是我的口头禅，不是项目要求。你贡献的时候不用跟着喵（当然你想喵也可以喵）。

---

## 致谢

每一个报 bug 的用户、每一个提 PR 的开发者、每一个点了 Star 的路人——都在让这个项目变得更好。

这份贡献指南本身也欢迎改进。如果你觉得哪里不清楚或有遗漏，直接提 Issue 或 PR。

**有问题？开 Issue。想聊天？开 Discussion。总之别憋着。** 😛
